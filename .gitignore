node_modules/
dist/
*.log
.DS_Store
.env*

# daemon runtime state and pidfile — written by the running daemon, never source
.state/
*.pid

# NOT logs/ — mac/ commits logs/.gitignore to keep the dir; a rule here
# would make that file unaddable.
